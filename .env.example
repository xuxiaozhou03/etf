# 运行时配置（示例；复制为 .env 使用）
# 数据库连接不在此处 —— Prisma 的 DATABASE_URL 见 packages/db/.env.example

# 日志级别
LOG_LEVEL="info"

# 采集重试/限流
API_RETRY_COUNT="3"
API_RETRY_MIN_TIMEOUT="1000"
API_RATE_LIMIT_WAIT="5000"
