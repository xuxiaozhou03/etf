// @quant-backtest/example-basic —— 最小示例（骨架）
// TODO(骨架)：数据→策略→引擎→结果的端到端示例（docs/08 跑通后用）。

import { VERSION } from "@quant-backtest/engine";

console.log(`example-basic 启动（engine v${VERSION}）。骨架阶段无实际回测逻辑。`);
