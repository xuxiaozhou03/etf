{
  "name": "@quant-backtest/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "可运行层：面向人执行的入口（数据同步 sync、最小示例 basic），非库、无 bin",
  "scripts": {
    "sync": "tsx src/sync.ts",
    "basic": "tsx src/basic.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@quant-backtest/crawler": "workspace:*",
    "@quant-backtest/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
