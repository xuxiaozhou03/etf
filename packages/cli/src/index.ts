#!/usr/bin/env node
// @quant-backtest/cli —— 回测命令行入口（骨架）
// TODO(骨架)：实现子命令（run / list / compare），见 docs/01 与 docs/08。

const VERSION = "0.1.0";

function main(): void {
  // TODO(骨架): 解析 argv → 装配 engine → 输出结果报告
  throw new Error("TODO(骨架): cli 尚未实现");
}

main();
